components:
    schemas:
        ListPluginData:
            properties:
                has_more:
                    description: "是否已返回全部消息。 \n  \n * true：未返回全部信息，可再次调用此接口查看其他分页。 \n * false：已返回全部信息。"
                    type: boolean
                items:
                    description: 插件列表，包含多个插件项的数组。每个插件项包含插件的元信息和运行统计信息。
                    items:
                        $ref: '#/components/schemas/ProductPluginInfo'
                    type: array
            type: object
        PluginInfo:
            properties:
                associated_bots_use_count:
                    description: 当前扣子商店中关联了该插件的智能体数量。
                    format: int64
                    type: integer
                avg_exec_duration_ms:
                    description: 插件执行的平均耗时，单位为毫秒。
                    format: double
                    type: number
                bots_use_count:
                    description: 该插件在智能体或工作流中的累计关联次数。
                    format: int64
                    type: integer
                call_count:
                    description: 插件的调用量，表示该插件的累计调用次数。
                    format: int64
                    type: integer
                description:
                    description: 插件的详细描述信息，用于说明插件的功能、用途和特点。
                    type: string
                favorite_count:
                    description: 插件的收藏量，表示该插件被用户收藏的总次数。
                    type: integer
                heat:
                    description: 插件的热度值，用于表示该插件的受欢迎程度或使用频率。数值越大表示热度越高。
                    type: integer
                is_call_available:
                    description: "标识该插件当前是否可被调用。 \n  \n * true：插件可正常调用。 \n * false：插件暂时不可用，例如已下架。"
                    type: boolean
                success_rate:
                    description: 插件的调用成功率，以小数形式表示，数值范围为 0 到 1。
                    format: double
                    type: number
                total_tools_count:
                    description: 插件包含的工具总数。
                    type: integer
            type: object
        ProductCategory:
            properties:
                id:
                    description: 插件分类 ID。
                    type: string
                name:
                    description: 插件分类名称。
                    type: string
            type: object
        ProductMetainfo:
            properties:
                category:
                    $ref: '#/components/schemas/ProductCategory'
                    description: 插件所属的分类信息，包含分类 ID 和分类名称。
                description:
                    description: 插件的描述信息，用于说明插件的功能、用途和特点。
                    type: string
                entity_id:
                    description: 插件 ID。
                    type: string
                entity_type:
                    description: 实体类型，当前仅支持 plugin，表示插件。
                    type: string
                entity_version:
                    description: 插件的版本号，用于标识插件的当前版本。
                    type: string
                icon_url:
                    description: 插件的图标 URL。
                    type: string
                is_official:
                    description: "标识插件是否为官方发布。 \n  \n * true 表示该插件由扣子编程官方发布。 \n * false表示由第三方开发者发布。"
                    type: boolean
                listed_at:
                    description: 插件上架时间，以 Unix 时间戳格式表示，单位为秒。
                    format: int64
                    type: integer
                name:
                    description: 插件的名称。
                    type: string
                paid_type:
                    description: 插件的付费类型，固定为 paid，即付费插件。
                    type: string
                product_id:
                    description: 商品的 ID，用于在插件商店中唯一标识该插件商品。
                    type: string
                user_info:
                    $ref: '#/components/schemas/ProductUserInfo'
                    description: 插件开发者的相关信息，包含开发者的用户 ID、昵称、用户名和头像 URL。
            type: object
        ProductPluginInfo:
            properties:
                metainfo:
                    $ref: '#/components/schemas/ProductMetainfo'
                    description: 插件的详细信息。
                plugin_info:
                    $ref: '#/components/schemas/PluginInfo'
                    description: 插件的运行数据和统计信息，包括热度、调用次数、成功率、收藏数等指标，以及被智能体使用的情况。
            type: object
        ProductUserInfo:
            properties:
                avatar_url:
                    description: 插件开发者在扣子编程上的头像 URL，用于展示开发者的头像信息。
                    type: string
                nick_name:
                    description: 插件开发者在扣子编程上的昵称。
                    type: string
                user_id:
                    description: 插件开发者的扣子用户 ID。
                    format: int64
                    type: integer
                user_name:
                    description: 插件开发者在扣子编程上的用户名。
                    type: string
            type: object
        ResponseDetail:
            properties:
                logid:
                    description: 本次请求的日志 ID。如果遇到异常报错场景，且反复重试仍然报错，可以根据此 logid 及错误码联系扣子团队获取帮助。详细说明可参考获取帮助和技术支持。
                    type: string
            type: object
info:
    title: 查询付费插件列表
    version: 1.0.0
openapi: 3.0.3
paths:
    /v1/stores/plugins:
        get:
            description: 查询插件商店中的付费插件列表。
            operationId: get_list_plugin
            parameters:
                - description: 插件搜索的关键词，支持模糊匹配。
                  in: query
                  name: keyword
                  required: false
                  schema:
                    type: string
                - description: "是否为扣子编程官方插件。默认返回官方插件和三方插件。 \n  \n *  true ：官方插件。 \n *  false：三方插件。"
                  in: query
                  name: is_official
                  required: false
                  schema:
                    type: boolean
                - description: 插件分类 ID 列表，用于筛选指定多个分类下的插件。默认为空，即返回所有分类下的插件。可以通过查询插件分类API 获取对应的插件分类 ID。
                  in: query
                  name: category_ids
                  required: false
                  schema:
                    items:
                        type: string
                    type: array
                - description: 分页查询时的页码。默认为 1，即返回第一页数据。
                  in: query
                  name: page_num
                  required: false
                  schema:
                    type: integer
                - description: 每页返回的数据条数，用于分页查询。默认值为 20，最大值为 100。
                  in: query
                  name: page_size
                  required: false
                  schema:
                    type: integer
                - description: "排序类型，用于指定返回插件的排序方式。支持的排序方式如下所示： \n  \n * heat：最受欢迎。 \n * newest：最近发布。 \n * relative：相关性，仅用于搜索场景。"
                  in: query
                  name: sort_type
                  required: false
                  schema:
                    type: string
                - description: 用于验证客户端身份的访问令牌。你可以在扣子编程中生成访问令牌，详细信息，参考准备工作。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 解释请求正文的方式。
                  in: header
                  name: Content-Type
                  required: false
                  schema:
                    type: string
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    code:
                                        description: 调用状态码。0 表示调用成功，其他值表示调用失败，你可以通过 msg 字段判断详细的错误原因。
                                        format: int64
                                        type: integer
                                    data:
                                        $ref: '#/components/schemas/ListPluginData'
                                        description: 返回的插件列表及其相关信息的对象。
                                    detail:
                                        $ref: '#/components/schemas/ResponseDetail'
                                        description: 包含请求的详细信息的对象，主要用于记录请求的日志 ID 以便于排查问题。
                                    msg:
                                        description: "状态信息。API 调用失败时可通过此字段查看详细错误信息。 \n 状态码为 0 时，msg 默认为空。"
                                        type: string
                                type: object
                    description: OK
            summary: 查询付费插件列表
            x-source-url: https://docs.coze.cn/api/open/docs/developer_guides/list_plugin
servers:
    - url: https://api.coze.cn
