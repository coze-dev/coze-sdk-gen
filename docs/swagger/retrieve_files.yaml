components:
    schemas:
        File:
            properties:
                bytes:
                    description: 文件的总字节数。
                    format: int64
                    type: integer
                created_at:
                    description: 文件的上传时间，格式为 10 位的 Unixtime 时间戳，单位为秒（s）。
                    format: int64
                    type: integer
                file_name:
                    description: 文件名称。
                    type: string
                id:
                    description: 已上传的文件 ID。
                    type: string
            type: object
        ResponseDetail:
            properties:
                logid:
                    description: 本次请求的日志 ID。如果遇到异常报错场景，且反复重试仍然报错，可以根据此 logid 及错误码联系扣子团队获取帮助。详细说明可参考获取帮助和技术支持。
                    type: string
            type: object
info:
    title: 查看文件详情
    version: 1.0.0
openapi: 3.0.3
paths:
    /v1/files/retrieve:
        get:
            description: 查看已上传的文件详情。
            operationId: get_retrieve_files
            parameters:
                - description: 已上传的文件 ID。你可以通过上传文件 API 的返回信息中查看文件 ID。
                  in: query
                  name: file_id
                  required: true
                  schema:
                    type: string
                - description: 用于验证客户端身份的访问令牌。你可以在扣子编程中生成访问令牌，详细信息，参考准备工作。
                  in: header
                  name: Authorization
                  required: false
                  schema:
                    type: string
                - description: 解释请求正文的方式。
                  in: header
                  name: Content-Type
                  required: false
                  schema:
                    type: string
            responses:
                "200":
                    content:
                        application/json:
                            schema:
                                properties:
                                    code:
                                        description: 调用状态码。0 表示调用成功，其他值表示调用失败，你可以通过 msg 字段判断详细的错误原因。
                                        format: int64
                                        type: integer
                                    data:
                                        $ref: '#/components/schemas/File'
                                        description: 已上传的文件信息，包含文件 ID、文件大小、文件名和上传时间等详细信息。
                                    detail:
                                        $ref: '#/components/schemas/ResponseDetail'
                                        description: 响应详情信息。
                                    msg:
                                        description: "状态信息。API 调用失败时可通过此字段查看详细错误信息。 \n 状态码为 0 时，msg 默认为空。"
                                        type: string
                                type: object
                    description: OK
            summary: 查看文件详情
            x-source-url: https://docs.coze.cn/api/open/docs/developer_guides/retrieve_files
servers:
    - url: https://api.coze.cn
